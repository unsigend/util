#include "tests/bool.h"
// #include "tests/double.h"
// #include "tests/edge.h"
// #include "tests/int.h"
// #include "tests/integration.h"
// #include "tests/long.h"
// #include "tests/string.h"

UTEST_SUITE(argparse)
{
  /* basic tests */
  UTEST_RUNCASE(bool);
  // UTEST_RUNCASE(int);
  // UTEST_RUNCASE(long);
  // UTEST_RUNCASE(string);
  // UTEST_RUNCASE(double);
  // UTEST_RUNCASE(edge);

  /* integration tests */
  // UTEST_RUNCASE(gcc);
  // UTEST_RUNCASE(as);
  // UTEST_RUNCASE(ld);
  // UTEST_RUNCASE(readelf);
}